{
  "private": true,
  "keywords": [
    "quip"
  ],
  "description": "Quip Live Apps API for creating applications in Quip documents",
  "repository": "quip/quip-apps",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/quip/quip-apps/issues"
  },
  "devDependencies": {
    "babel-eslint": "^8.0.1",
    "eslint": "^4.9.0",
    "lerna": "^3.16.4",
    "prettier": "^1.7.4"
  },
  "dependencies": {},
  "scripts": {
    "build": "lerna run build",
    "version": "lerna version",
    "release:next": "lerna publish prerelease --dist-tag next",
    "release": "lerna publish from-git",
    "release:ci": "lerna run build && npm run release:next -- --yes && npm run release --yes",
    "postinstall": "lerna bootstrap",
    "test": "lerna bootstrap && lerna exec -- npm run build --if-present && lerna run test"
  }
}
