{
  "private": true,
  "keywords": [
    "quip"
  ],
  "description": "Quip Live Apps API for creating applications in Quip documents",
  "repository": "quip/quip-apps",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/quip/quip-apps/issues"
  },
  "devDependencies": {
    "babel-eslint": "^8.0.1",
    "eslint": "^4.9.0",
    "lerna": "^3.16.4",
    "prettier": "^1.7.4"
  },
  "dependencies": {},
  "scripts": {
    "publish-all:next": "lerna publish prerelease --dist-tag next",
    "publish-all": "lerna publish from-git",
    "publish-all:ci": "lerna run build && npm run publish-all:next -- --yes && npm run publish-all -- --yes",
    "postinstall": "lerna bootstrap",
    "test": "lerna exec -- npm run build --if-present && lerna run test"
  }
}
